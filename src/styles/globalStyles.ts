import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
  whiteText: {
    color: "#fff",
  },
  darkText: {
    color: "#000",
  },
  whiteContainer: {
    backgroundColor: "#fff",
  },
  darkContainer: {
    backgroundColor: "#000",
  },
});
